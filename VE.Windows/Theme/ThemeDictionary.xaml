<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- VE Brand Colors -->
    <Color x:Key="VEBlue">#007CEC</Color>
    <Color x:Key="VERed">#FF4B59</Color>
    <Color x:Key="VEYellow">#FFC600</Color>
    <Color x:Key="VEGreen">#00CA48</Color>
    <Color x:Key="VETextSecondary">#878E92</Color>
    <Color x:Key="VEWhite">#FFFFFF</Color>
    <Color x:Key="VEBlack">#000000</Color>

    <!-- Dark Theme Colors -->
    <Color x:Key="DarkBackground">#151719</Color>
    <Color x:Key="DarkCard">#25292D</Color>
    <Color x:Key="DarkTextPrimary">#F4F5F5</Color>
    <Color x:Key="DarkBorder">#1AF4F5F5</Color>

    <!-- Light Theme Colors -->
    <Color x:Key="LightBackground">#F4F5F5</Color>
    <Color x:Key="LightCard">#FFFFFF</Color>
    <Color x:Key="LightTextPrimary">#272B30</Color>
    <Color x:Key="LightBorder">#1A000000</Color>

    <!-- Default Brushes (Dark mode default) -->
    <SolidColorBrush x:Key="VEBlueBrush" Color="{StaticResource VEBlue}" />
    <SolidColorBrush x:Key="VEBlueHoverBrush" Color="{StaticResource VEBlue}" Opacity="0.8" />
    <SolidColorBrush x:Key="VERedBrush" Color="{StaticResource VERed}" />
    <SolidColorBrush x:Key="VEYellowBrush" Color="{StaticResource VEYellow}" />
    <SolidColorBrush x:Key="VEGreenBrush" Color="{StaticResource VEGreen}" />
    <SolidColorBrush x:Key="VETextSecondaryBrush" Color="{StaticResource VETextSecondary}" />
    <SolidColorBrush x:Key="VEWhiteBrush" Color="{StaticResource VEWhite}" />
    <SolidColorBrush x:Key="VEBlackBrush" Color="{StaticResource VEBlack}" />

    <!-- Notch Colors -->
    <SolidColorBrush x:Key="NotchBackground" Color="#0D0D0D" />
    <SolidColorBrush x:Key="NotchBorderBrush" Color="#33FFFFFF" />
    <SolidColorBrush x:Key="LoginBackgroundBrush" Color="{StaticResource VEBlue}" />

    <!-- Font Family -->
    <FontFamily x:Key="VEFont">Segoe UI</FontFamily>
    <FontFamily x:Key="VEFontMedium">Segoe UI Semibold</FontFamily>

    <!-- Font Sizes -->
    <sys:Double x:Key="FontSizeH1" xmlns:sys="clr-namespace:System;assembly=mscorlib">24</sys:Double>
    <sys:Double x:Key="FontSizeH2" xmlns:sys="clr-namespace:System;assembly=mscorlib">20</sys:Double>
    <sys:Double x:Key="FontSizeH3" xmlns:sys="clr-namespace:System;assembly=mscorlib">18</sys:Double>
    <sys:Double x:Key="FontSizeTitle" xmlns:sys="clr-namespace:System;assembly=mscorlib">15</sys:Double>
    <sys:Double x:Key="FontSizeParagraph" xmlns:sys="clr-namespace:System;assembly=mscorlib">14</sys:Double>
    <sys:Double x:Key="FontSizeDescription" xmlns:sys="clr-namespace:System;assembly=mscorlib">12</sys:Double>
    <sys:Double x:Key="FontSizeButton" xmlns:sys="clr-namespace:System;assembly=mscorlib">14</sys:Double>
    <sys:Double x:Key="FontSizeSmall" xmlns:sys="clr-namespace:System;assembly=mscorlib">11</sys:Double>

    <!-- Corner Radius -->
    <CornerRadius x:Key="SmallRadius">4</CornerRadius>
    <CornerRadius x:Key="MediumRadius">8</CornerRadius>
    <CornerRadius x:Key="LargeRadius">12</CornerRadius>
    <CornerRadius x:Key="NotchRadius">16</CornerRadius>
    <CornerRadius x:Key="RoundRadius">20</CornerRadius>

    <!-- Base Button Style -->
    <Style x:Key="VEButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource VEBlueBrush}" />
        <Setter Property="Foreground" Value="{StaticResource VEWhiteBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource VEFontMedium}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeButton}" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource MediumRadius}"
                            Padding="{TemplateBinding Padding}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.7" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary Button Style -->
    <Style x:Key="VESecondaryButton" TargetType="Button" BasedOn="{StaticResource VEButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource VEBlueBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource VEBlueBrush}" />
    </Style>

    <!-- Ghost Button -->
    <Style x:Key="VEGhostButton" TargetType="Button" BasedOn="{StaticResource VEButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource VETextSecondaryBrush}" />
    </Style>

    <!-- TextBox Style -->
    <Style x:Key="VETextBox" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource VEFont}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeParagraph}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource MediumRadius}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollBar Style (Thin, auto-hide) -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="6" />
        <Setter Property="MinWidth" Value="6" />
        <Setter Property="Opacity" Value="0.3" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="0.7" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Card Border Style -->
    <Style x:Key="VECard" TargetType="Border">
        <Setter Property="CornerRadius" Value="{StaticResource LargeRadius}" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!-- Storyboard Animations -->
    <Storyboard x:Key="FadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="FadeOut">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="1" To="0" Duration="0:0:0.15">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseIn" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="SlideDown">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                         From="-20" To="0" Duration="0:0:0.35">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.25" />
    </Storyboard>

    <Storyboard x:Key="SpringOpen">
        <DoubleAnimation Storyboard.TargetProperty="Height"
                         Duration="0:0:0.4">
            <DoubleAnimation.EasingFunction>
                <ElasticEase EasingMode="EaseOut" Oscillations="1" Springiness="5" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

</ResourceDictionary>
