<UserControl x:Class="VE.Windows.Views.Components.AudioWaveBars"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="60" Height="30">

    <Canvas x:Name="WaveCanvas" VerticalAlignment="Bottom">
        <!-- Bars are created dynamically in code-behind for configurable count -->
    </Canvas>
</UserControl>
